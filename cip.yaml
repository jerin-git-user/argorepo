apiVersion: v1
kind: Service
metadata:
  name: svc-cv
  namespace: jerinraj
spec:
  type: ClusterIp
  selector:
    app: nginx
  ports:
  - port: 80  #service port inside cluster
    targetPort: 80 #container port
